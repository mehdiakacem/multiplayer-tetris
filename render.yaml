services:
  - name: multiplayer-tetris
    type: web
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm ci && npm run build:client
    startCommand: npm run start:server
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
